{
  "name": "my-postit",
  "version": "1.0.0",
  "description": "A simple desktop sticky note application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "build": "electron-packager . MyPostit --platform=win32 --arch=x64 --out=dist --overwrite --ignore=dist --ignore=node_modules/.cache"
  },
  "keywords": [
    "electron",
    "sticky",
    "note",
    "desktop"
  ],
  "author": "Myself",
  "license": "MIT",
  "devDependencies": {
    "@electron/packager": "^18.0.0",
    "electron": "^28.0.0"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "marked": "^12.0.2"
  }
}
